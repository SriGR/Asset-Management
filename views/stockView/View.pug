doctype html
html
  head
    title Stock View
    link(rel='stylesheet', href='/styles/style.css')
  body

  
    div.pageParentDiv
     div.sideBarSection
      div.sideBarHeadSec
        | KT Telematic 
      div.sideBarListDiv 
       ul.sidebarListUlstyle
        li: a(href='/home').activeList Home
        li: a(href='/assets').NonActiveList Asset
        li: a(href='/asset-category').NonActiveList Asset Category
        li: a(href='/asset-history').NonActiveList Asset History
        li: a(href='/employee').NonActiveList Employee
        li: a(href='/issue-asset').NonActiveList Issue Asset
        li: a(href='/return-asset').NonActiveList Return Asset
        li: a(href='/scrap-asset').NonActiveList Scrap Asset
     div.pageRenderSection
      span.tableTitle Stock View
      div.tableContainer
        table.table.table-bordered
          thead
            tr
              th Asset ID
              th Asset Name
              th Asset Number
              th Branch
              th Purchase Amount
              th Status
          tbody
            each asset in availableAssets
              tr
                td= asset.id
                td= asset.assetName
                td= asset.assetNumber
                td= asset.branchName
                td= asset.purchaseAmount
                td= asset.status

      span.tableTitle Branch Totals
      div.tableContainer
        table.table.table-bordered
          thead
            tr
              th Branch
              th Total Count
              th Total Value
          tbody
            each branch in Object.keys(branchTotals)
              tr
                td= branch
                td= branchTotals[branch].totalCount
                td= '$' + branchTotals[branch].totalValue.toFixed(2)

